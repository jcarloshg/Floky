import 'package:flutter/material.dart';

class Recomendation {
  final String title;
  final String subtitle;
  final String text;
  final Color color;

  Recomendation(this.title, this.subtitle, this.text, this.color);
}
